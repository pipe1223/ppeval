# Shared evaluation utilities

This directory contains reusable logic shared across visual, retrieval, RAG, GraphRAG, and online evaluation.

Main areas:
- `metrics/` for reusable scoring functions
- `schemas/` for shared dataclasses and review formats
- `utils/` for file and normalization helpers
