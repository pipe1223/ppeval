# Shared schemas

This directory is reserved for reusable input and output schemas.

Examples:
- common report JSON schema
- retrieval sample schema
- evaluation summary schema
- multimodal result schema

The goal is to keep data formats consistent across visual and retrieval evaluation.
