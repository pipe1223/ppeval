# Shared utils

This directory is reserved for helper utilities reused across evaluation modules.

Examples:
- JSON loading helpers
- result formatting helpers
- report merging
- path normalization
- simple CLI helpers

Utilities should move here only when they are genuinely shared across multiple evaluation targets.
