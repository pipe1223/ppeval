# Visual evaluation

This directory contains visual and VLM evaluation modules.

The original implementation has been preserved under `visual/legacy/` so older scripts and results can keep working while the wider PP Evaluation project grows.
